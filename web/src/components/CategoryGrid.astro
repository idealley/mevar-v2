---
import WorkCard from "./WorkCard.astro";
import type { WorkEntry } from "$lib/works";

interface Props {
  entries: WorkEntry[];
  title: string;
  description?: string;
}

const { entries, title, description } = Astro.props;
---

<section class="container mx-auto px-4 py-10">
  <header class="mb-8 max-w-2xl">
    <h1 class="text-3xl md:text-4xl font-bold tracking-tight mb-2">{title}</h1>
    {description && <p class="text-muted-foreground">{description}</p>}
    <p class="text-sm text-muted-foreground mt-2">{entries.length.toLocaleString("fr-FR")} document{entries.length > 1 ? "s" : ""}</p>
  </header>

  {entries.length === 0 ? (
    <p class="text-muted-foreground">Aucun document pour le moment.</p>
  ) : (
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {entries.map((entry) => <WorkCard entry={entry} />)}
    </div>
  )}
</section>
