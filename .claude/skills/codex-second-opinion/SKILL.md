---
name: codex-second-opinion
description: Second opinion on a branch of mevar-v2 from another model family, through the Codex CLI in read-only mode. Use after DELIVERY step 4's independent review said ACCEPT and before the PR (or on an open PR, before Samuel merges); also when Samuel asks for a second opinion on merged or in-flight code, or when the Codex GitHub app is silent on a PR. Advisory: findings are fixed or declined with evidence; Codex never approves.
---

# Codex CLI as second reviewer

DELIVERY step 4's independent review is a fresh session of the same model
family as the implementer. This one is a different family. The CLI and the
Codex GitHub app draw on different quotas; when the app is silent on a PR,
this skill is also the fallback.

The review is ADVISORY. Every finding is verified against the code, then
fixed (script rerun, diff shown) or declined with evidence. "No findings" is
reported as "no findings", never as "approved".

## When

Once per branch: after the step 4 review said ACCEPT, before the PR or before
the merge. Run it again only after a fix batch that changed more than a few
lines, and then the changed code also goes back through one round of the
step 4 review. Two passes are the ceiling; what remains goes to the PR.

## Size: review the code, sample the data

A goal that regenerates the corpus changes hundreds of `markdown/` files,
`manifests/*.json` and `index.json`. No reviewer can read that diff, and one
that tries reviews nothing. So every prompt:

- names the hand-written files (`scripts/`, `docs/`, `web/src/`) as the
  surface to read in full;
- forbids reading the generated diff line by line (`markdown/`,
  `manifests/`, `index.json`, `web/dist/`);
- gives read-only commands that measure it instead: counts before and after
  (`git show origin/main:<file>` against the working file), a seeded random
  sample checked against the text, a `git diff --stat` by directory, and the
  script's `--dry` mode where it has one.

## Models: one pass per task size

Split the review into scoped passes and run them in parallel, each with the
model its task needs. Name it with `-m`; the default in `~/.codex/config.toml`
is only used when a task does not say.

| model | for | typical pass |
| - | - | - |
| `gpt-6-luna` | simple, mechanical checks | numbers in the docs, the goal file and the PR match the data; copy rules; every changed file is one the goal declares |
| `gpt-6-sol` | moderate: read and verify | sample the generated data against the text; the diff is limited to what the goal predicts; a second run is a no-op |
| `gpt-6-astra` | hard reasoning | attack the code: build inputs that break each property; what could be deleted; convergence and the revert test |

A small change needs only the astra pass. Never more than three passes a
round.

## Invocation

Always non-interactive, always read-only, always from the worktree's root.
Transcripts go in `.claude/codex-reviews/`, which `.gitignore` excludes;
they never enter a commit.

```bash
mkdir -p .claude/codex-reviews
codex exec -m <model> --sandbox read-only -C <worktree-root> \
  -o .claude/codex-reviews/<branch>-<pass>.md "<prompt>" \
  </dev/null > .claude/codex-reviews/<branch>-<pass>.log 2>&1
```

`</dev/null` is required: without it `codex exec` waits for more input on
stdin ("Reading additional input from stdin...") and never starts. Do not
edit the worktree while a pass runs; it reads the working tree, not a
commit.

`--sandbox read-only` is not optional: the reviewer must be structurally
unable to modify the tree. It can still run read-only commands (`git`,
`grep`, `node -e`, a script's `--dry`); never ask it to run a script that
writes. `-o` keeps the final report apart from the tool noise.

## Prompt shape

```
Second-opinion review of the branch <branch> against origin/main in this
repository. Read AGENTS.md (hard rules), DELIVERY.md (the reject list) and
the contract <goal file> first. Surface to read in full: <hand-written
files>. Do NOT read the diff of <generated paths>: it is regenerated by
<scripts>; measure it with read-only commands instead (<suggestions>).
Your pass: <the pass's task, with the 2 to 4 properties it checks>.
Attack it: try to construct an input that violates each property.
Then: has this converged? Report everything remaining, say which
categories are clean, and say which guards would survive being reverted
with the output unchanged.
Report findings only, each with file:line and a concrete violating input
or command output; change nothing. Open with what could be deleted.
```

Properties of this repository worth naming when the change is near them:

- a work's wording never changes outside an editorial goal (AGENTS.md hard
  rule 1): a pipeline change touches frontmatter or derived files only;
- Scripture text comes from data, never from a model's memory;
- a derived file (`manifests/*`, `index.json`, `web/dist/`) is regenerated
  by its script, never edited by hand, and the diff is explained by the
  script;
- every script is idempotent: a second run is a no-op, and it deletes only
  what it created;
- a draft is never built, listed, indexed or emailed;
- the live site has no runtime dependency; every asset is on our domain;
- no reader's personal data, no secret, no populated env file anywhere.

## Handling the output

1. Triage every finding. Real: fix on the branch, rerun the script, show
   the diff. Not real: decline with evidence in the commit message of the
   commit that answers the pass. A finding about breadth the goal did not
   ask for is declined on scope.
2. Put the result in the PR description under "Second opinion", after
   "Independent review", with one line on top per pass: "Codex CLI
   (`<model>`, read-only), <pass>: N findings, X fixed, Y declined", then
   each finding with what was done and the evidence.
3. If the CLI is quota-dead or errors, say so in the PR description and
   proceed; never fake the pass.

## Hard rules

- Never `--sandbox workspace-write` or full access for a review.
- Never put `.env`, member or subscriber data, or a Ghost export in the
  prompt. Paths under `scripts/`, `docs/`, `web/src/`, `markdown/` and
  `manifests/` are fine.
- Advisory only: no finding is auto-applied; the fix-or-decline judgment
  stays with the implementing session.
